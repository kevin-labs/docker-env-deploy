version: '3.3'
services:

  # 缓存
  redis:
    image: memcached:alpine
    network_mode: hosting_running_network
    expose:
      - 11211
    environment:
      - TZ=Asia/Shanghai
    # expose:
    #   - '127.0.0.1:11211:11211'
    restart: always
    container_name:   

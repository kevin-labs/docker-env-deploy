version: '3.3'
services:

  # 缓存
  redis:
    image: redis:3-alpine
    network_mode: hosting_running_network
    expose:
      - 6379
    environment:
      - TZ=Asia/Shanghai
    # expose:
    #   - '127.0.0.1:6379:6379'
    restart: always
    container_name: cache_redis